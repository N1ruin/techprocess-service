package by.niruin.techprocess_service.model.library;

public record SafetyInstructionDto(Long id,
                                   String number,
                                   String description) {
}